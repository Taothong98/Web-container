version: '3'
services:

  server:
    image: taothong/web:1.0
    container_name: apache2
    ports:
      - 80:80
    networks:
      - link        
    volumes:
      - ./www:/var/www/html

    # command: ["/bin/bash","-c","/home/link/loop.sh"]      
# ------------------------------------------------------------

  
networks:
  link:
    driver: bridge      